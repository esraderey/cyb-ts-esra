name: build_cyb_rebyc

on:
  push:
    branches:
      - dev

jobs:
  build_cyb:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Set up Node.js (for yarn install)
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: yarn install

      - name: Build cyb rebyc
        run: deno task build
        env:
          NODE_OPTIONS: '--max-old-space-size=8192'
          CHAIN_ID: 'bostrom'

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          token: ${{ secrets.ACTIONS_DEPLOY_ACCESS_TOKEN }}
          branch: rebyc-build
          folder: build
          clean: true
